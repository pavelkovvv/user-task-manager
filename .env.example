# Скопируй в .env и заполни значения:
# cp .env.example .env

POSTGRES_DB_USER=postgres
POSTGRES_DB_PASSWORD=change-me
POSTGRES_DB_HOST=localhost
POSTGRES_DB_PORT=5432
POSTGRES_DB_NAME=user-task-manager

# Отдельная БД для pytest (должна отличаться от POSTGRES_DB_NAME):
# createdb user-task-manager-test
# alembic upgrade head  # с POSTGRES_DB_NAME=user-task-manager-test
POSTGRES_TEST_DB_NAME=user-task-manager-test
